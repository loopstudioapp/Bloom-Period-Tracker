import SwiftUI

class SymptomService {
    static let shared = SymptomService()

    func logSymptoms(_ symptoms: Set<String>, for date: Date) {
        // Persistence placeholder
    }

    func logFlow(_ flow: MenstrualFlow?, for date: Date) {
        // Persistence placeholder
    }

    func logWater(_ oz: Int, for date: Date) {
        // Persistence placeholder
    }

    func logWeight(_ lbs: Double, for date: Date) {
        // Persistence placeholder
    }
}
